# Suri 自学习模块 — 完整需求规格书

> 版本: 1.0  
> 日期: 2026-04-30  
> 作者: Kimi Code CLI  
> 目标: 可直接交付给任意 AI 编码助手执行实现

---

## 1. 模块定位

**自学习模块（Learning Module）** 是 Suri Agent 平台的经验积累与进化系统。

- **角色自学习**：每个业务角色（设计、开发、写作等）在执行任务后，自动复盘提取经验，沉淀到角色私人记忆区。下次同类任务调度时，将相关经验注入 System Prompt，避免重复犯错、复用成功模式。
- **主程序自学习**：suri 核心调度器汇总全平台任务数据，持续优化部门匹配策略、用户画像、模型路由偏好。

**设计哲学**：
1. 异步非阻塞 — 学习过程在后台进行，不延迟用户响应
2. 文件即记忆 — 经验以 Markdown 形式存储，AI 可直接读写理解
3. 分层防御冗余 — 写入去重 + 夜间压缩 + 动态遗忘 + 注入截断
4. 角色自治 — 角色 A 的经验不可泄露给角色 B

---

## 2. 总体架构

### 2.1 数据流

```
┌─────────────────┐     任务完成/失败      ┌─────────────────────┐
│  TaskService    │ ────────────────────→ │  FeedbackCollector  │
│  (core/)        │                       │  (learning/)        │
└─────────────────┘                       └─────────────────────┘
                                                    │
                                                    ▼
                                          ┌─────────────────────┐
                                          │  RoleLearner        │
                                          │  (learning/)        │
                                          └─────────────────────┘
                                                    │
              ┌─────────────────────────────────────┼─────────────────────────────────────┐
              │                                     │                                     │
              ▼                                     ▼                                     ▼
    ┌─────────────────┐                 ┌─────────────────┐                 ┌─────────────────┐
    │ ExperienceExtractor│               │ MemoryService   │                 │ LoggerService   │
    │ (调用 LLM)       │                 │ (扩展方法)       │                 │ (扩展方法)       │
    └─────────────────┘                 └─────────────────┘                 └─────────────────┘
              │                                     │                                     │
              │         经验文本                      │         保存到文件                    │         记录日志
              │                                     │                                     │
              └─────────────────────────────────────┴─────────────────────────────────────┘
                                                        │
                                                        ▼
                                              ┌─────────────────┐
                                              │  group/<role>/  │
                                              │  memories/      │
                                              │  insights/*.md  │
                                              └─────────────────┘
                                                        │
              下次调度时                                │
              ┌─────────────────────────────────────────┘
              │
              ▼
    ┌─────────────────┐
    │ ContextService  │  build_context() 中注入经验段
    │ (core/)         │
    └─────────────────┘
              │
              ▼
    ┌─────────────────┐
    │ ModelService    │  带经验的 System Prompt
    │ (core/)         │
    └─────────────────┘
```

### 2.2 调用时序

```
1. 用户发送消息
2. TaskService.receive_task() → 创建任务
3. TaskService.dispatch() → 匹配部门 → 下发总监
4. 角色执行 → 返回结果
5. TaskService.dispatch() 成功返回前
   └── asyncio.create_task(_trigger_learning(task_id, role_id, success=True))
       └── RoleLearner.learn_from_task() [后台异步]
           ├── 读取任务消息链 (MemoryService)
           ├── 组装分析 Prompt (ExperienceExtractor)
           ├── 调用 LLM (ModelService)
           ├── 解析结果
           ├── 去重检查
           ├── 写入经验文件 (MemoryService.save_role_insight)
           └── 记录日志 (LoggerService.log_learning)
6. 用户收到结果（步骤5 在后台，不阻塞）
7. 下次同角色调度时
   └── ContextService.build_context()
       └── 注入最近相关经验 (MemoryService.get_recent_insights_for_context)
```

---

## 3. 新增文件清单（7 个）

### 3.1 `suri-agent/learning/__init__.py`

```python
"""自学习模块入口"""
from .role_learner import RoleLearner
from .platform_learner import PlatformLearner
from .feedback_collector import FeedbackCollector
from .experience_extractor import ExperienceExtractor

__all__ = ['RoleLearner', 'PlatformLearner', 'FeedbackCollector', 'ExperienceExtractor']
```

### 3.2 `suri-agent/learning/base.py`

```python
"""学习器基类"""
from abc import ABC, abstractmethod
from typing import Dict, Any, Optional


class BaseLearner(ABC):
    """所有学习器的抽象基类"""
    
    learner_id: str = ""
    name: str = ""
    version: str = "1.0.0"
    
    @abstractmethod
    async def learn(self, context: Dict[str, Any]) -> Optional[str]:
        """
        执行学习，返回学习成果文本
        
        Args:
            context: 包含任务ID、角色ID、状态等信息的字典
            
        Returns:
            经验文本，或 None（无可学习的内容）
        """
        pass
    
    def describe(self) -> str:
        return f"[{self.learner_id}] {self.name} v{self.version}"
```

### 3.3 `suri-agent/learning/feedback_collector.py`

```python
"""
反馈收集器

职责：
- 在任务生命周期关键节点收集结构化反馈
- 为学习器提供标准化的输入数据

触发点：
1. 任务完成（成功/失败/取消）
2. 用户显式反馈（确认/修改/拒绝）
3. 异常重试耗尽
"""

from typing import Dict, Any, Optional
from dataclasses import dataclass
from enum import Enum


class TaskOutcome(str, Enum):
    SUCCESS = "success"      # 任务完成，用户满意
    PARTIAL = "partial"      # 完成但需修改
    FAILED = "failed"        # 执行失败
    CANCELLED = "cancelled"  # 用户取消
    TIMEOUT = "timeout"      # 超时


class UserFeedback(str, Enum):
    NONE = "none"            # 无显式反馈
    CONFIRM = "confirm"      # 用户确认通过
    MODIFY = "modify"        # 用户要求修改
    REJECT = "reject"        # 用户拒绝
    PRAISE = "praise"        # 用户表扬
    COMPLAIN = "complain"    # 用户抱怨


@dataclass
class FeedbackRecord:
    """标准化反馈记录"""
    task_id: str
    role_id: str
    session_id: str
    outcome: TaskOutcome
    user_feedback: UserFeedback = UserFeedback.NONE
    retry_count: int = 0
    execution_time_ms: int = 0
    user_comment: str = ""   # 用户自由文本反馈
    error_info: str = ""     # 失败时的错误信息


class FeedbackCollector:
    """反馈收集器"""
    
    def __init__(self, memory_service, logger=None):
        self.memory = memory_service
        self.logger = logger
    
    def collect_task_feedback(self, task_id: str, role_id: str) -> FeedbackRecord:
        """
        从 role.db 中提取任务的完整反馈信息
        
        读取内容：
        - task 记录（状态、重试次数）
        - 消息链（完整对话）
        - 审批记录（如有）
        
        Returns:
            FeedbackRecord 结构化对象
        """
        pass
    
    def record_user_feedback(self, task_id: str, feedback: UserFeedback, comment: str = "") -> None:
        """
        记录用户的显式反馈（由接入层调用）
        
        存储位置：在对应 role.db 的 messages 表中新增一条 type='user_feedback' 的消息
        """
        pass
```

### 3.4 `suri-agent/learning/experience_extractor.py`

```python
"""
经验提取器

职责：
- 定义经验提取的 Prompt 模板
- 调用 LLM 分析任务记录，生成结构化经验
- 解析 LLM 输出为内部格式

原则：Prompt 工程集中在此处，便于统一调优。
"""

from typing import Dict, Any, List
from dataclasses import dataclass
from datetime import datetime


@dataclass
class ExtractedInsight:
    """提取后的经验条目"""
    title: str              # 经验标题（一句话概括）
    category: str           # 类别: success_pattern / improvement / pitfall / preference
    situation: str          # 适用情境
    key_point: str          # 核心要点
    avoid: str              # 避免什么
    confidence: float = 0.5  # 置信度 0-1
    created_at: str = ""     # ISO 时间


class ExperienceExtractor:
    """经验提取器"""
    
    def __init__(self, model_service):
        self.model = model_service
    
    async def extract(self, feedback: 'FeedbackRecord', message_chain: List[Dict]) -> List[ExtractedInsight]:
        """
        从任务反馈中提取经验
        
        Args:
            feedback: 结构化反馈记录
            message_chain: 完整消息链（角色对话记录）
            
        Returns:
            0-3 条 ExtractedInsight，空列表表示无可学习的内容
        """
        prompt = self._build_prompt(feedback, message_chain)
        response = await self.model.call_model_async(prompt)
        return self._parse_response(response)
    
    def _build_prompt(self, feedback, message_chain) -> str:
        """构建经验提取 Prompt"""
        pass
    
    def _parse_response(self, response: str) -> List[ExtractedInsight]:
        """解析 LLM 返回的文本为结构化经验"""
        pass


# ═══════════════════════════════════════════════════════
# Prompt 模板（必须在 _build_prompt 中使用）
# ═══════════════════════════════════════════════════════

EXPERIENCE_EXTRACTION_PROMPT_TEMPLATE = """你是一位经验丰富的 AI 助手复盘专家。请分析以下任务执行记录，提取 0-3 条可复用的经验。

【任务信息】
角色: {role_id}
任务ID: {task_id}
状态: {outcome}
用户反馈: {user_feedback}
重试次数: {retry_count}
执行耗时: {execution_time_ms}ms
用户评论: {user_comment}
错误信息: {error_info}

【完整对话记录】
{message_chain}

【指令】
请判断这次任务是否有值得记录的经验：
- 如果任务成功且顺利，提取"成功模式"
- 如果用户反复修改或反馈负面，提取"改进建议"和"常见陷阱"
- 如果任务失败，分析决策链中哪一步出了问题
- 如果只是普通任务、无明显特征，返回"无"

【输出格式】（严格按此格式，不要多余内容）
---
经验1
类别: [success_pattern / improvement / pitfall / preference]
标题: [一句话概括，20字以内]
情境: [什么情况下适用]
要点: [具体做法，100字以内]
避免: [如果做错了会怎样，50字以内]
---
经验2
类别: ...
...
---

如果没有值得记录的经验，只输出一个字：无"""


# 解析规则（必须在 _parse_response 中实现）：
# 1. 按 "---" 分割文本
# 2. 跳过标题为空的段落
# 3. 提取 category, title, situation, key_point, avoid
# 4. confidence 初始值：success_pattern=0.7, improvement=0.6, pitfall=0.8, preference=0.5
# 5. created_at 使用当前 ISO 时间
```

### 3.5 `suri-agent/learning/role_learner.py`

```python
"""
角色自学习引擎

职责：
- 任务完成后，分析该角色的任务记录
- 提取经验、去重、写入角色记忆区
- 触发点：TaskService._trigger_learning()

设计原则：
- 异步执行，不阻塞主流程
- 错误处理完善，学习失败不影响系统运行
"""

import asyncio
from pathlib import Path
from typing import Optional, Dict, Any
from datetime import datetime

from .base import BaseLearner
from .feedback_collector import FeedbackCollector, FeedbackRecord
from .experience_extractor import ExperienceExtractor, ExtractedInsight
from infrastructure.memory import MemoryService
from infrastructure.logger import LoggerService
from core.model_router import ModelService


class RoleLearner(BaseLearner):
    """角色自学习引擎"""
    
    learner_id = "role_learner"
    name = "角色自学习引擎"
    version = "1.0.0"
    
    def __init__(self, memory: MemoryService, model: ModelService, 
                 logger: Optional[LoggerService] = None):
        self.memory = memory
        self.model = model
        self.logger = logger
        self.collector = FeedbackCollector(memory, logger)
        self.extractor = ExperienceExtractor(model)
    
    async def learn(self, context: Dict[str, Any]) -> Optional[str]:
        """
        执行角色学习
        
        Args:
            context: {
                'task_id': str,
                'role_id': str,
                'success': bool
            }
        """
        return await self.learn_from_task(
            context['role_id'], 
            context['task_id']
        )
    
    async def learn_from_task(self, role_id: str, task_id: str) -> Optional[str]:
        """
        核心方法：从单个任务中学习
        
        步骤：
        1. 收集反馈
        2. 提取经验
        3. 去重检查
        4. 写入文件
        5. 记录日志
        """
        try:
            # 1. 收集反馈
            feedback = self.collector.collect_task_feedback(task_id, role_id)
            message_chain = self._get_message_chain(role_id, task_id)
            
            if self.logger:
                self.logger.log_learning(role_id, "开始复盘", f"任务 {task_id}")
            
            # 2. 提取经验
            insights = await self.extractor.extract(feedback, message_chain)
            
            if not insights:
                if self.logger:
                    self.logger.log_learning(role_id, "无经验可提取", f"任务 {task_id}")
                return None
            
            # 3. 逐条去重并写入
            saved_count = 0
            for insight in insights:
                if await self._is_duplicate(role_id, insight):
                    # 合并到已有经验
                    await self._merge_insight(role_id, insight)
                    if self.logger:
                        self.logger.log_learning(role_id, "经验合并", insight.title)
                else:
                    # 写入新经验
                    await self._save_insight(role_id, insight)
                    saved_count += 1
                    if self.logger:
                        self.logger.log_learning(role_id, "新经验保存", insight.title)
            
            if self.logger:
                self.logger.log_learning(
                    role_id, 
                    "复盘完成", 
                    f"任务 {task_id} | 提取 {len(insights)} 条 | 新增 {saved_count} 条"
                )
            
            return f"已保存 {saved_count} 条新经验"
            
        except Exception as e:
            if self.logger:
                self.logger.error("自学习", f"角色 {role_id} 任务 {task_id} 复盘失败: {e}")
            return None
    
    def _get_message_chain(self, role_id: str, task_id: str) -> list:
        """从 role.db 读取任务的完整消息链"""
        return self.memory.get_task_messages(role_id, task_id)
    
    async def _is_duplicate(self, role_id: str, insight: ExtractedInsight) -> bool:
        """
        去重检查：判断该经验是否已存在
        
        方案 A 实现（轻量）：
        - 读取该角色最近 50 条经验
        - 标题关键词杰卡德相似度 > 0.6 视为重复
        - 或 content 包含相同核心短语（5字以上短语匹配）
        
        Returns:
            True = 存在重复，需要合并
        """
        pass
    
    async def _merge_insight(self, role_id: str, insight: ExtractedInsight) -> None:
        """
        合并经验：将新洞察合并到已有经验文件中
        
        操作：
        1. 找到匹配的旧经验文件
        2. 更新 trigger_count +1
        3. 更新 last_triggered 时间
        4. 如内容有补充，追加到"验证记录"段
        """
        pass
    
    async def _save_insight(self, role_id: str, insight: ExtractedInsight) -> str:
        """
        保存新经验到角色记忆区
        
        文件路径: group/<role>/memories/insights/YYYYMMDD_HHMMSS_{sanitized_title}.md
        
        文件内容格式见第 5 节"经验文件格式"
        """
        pass
```

### 3.6 `suri-agent/learning/platform_learner.py`

```python
"""
主程序自学习引擎

职责：
- 汇总全平台角色任务数据
- 优化 suri 的调度策略、用户画像、模型路由
- 周期性复盘（夜间定时执行）

第一期实现要求：
- 搭好框架和接口
- 实现 daily_review() 的空壳（仅读取日志，不实际处理）
- 预留扩展点，不阻塞主流程
"""

from typing import Dict, Any, List, Optional
from .base import BaseLearner
from infrastructure.memory import MemoryService
from infrastructure.logger import LoggerService
from core.model_router import ModelService


class PlatformLearner(BaseLearner):
    """主程序自学习引擎"""
    
    learner_id = "platform_learner"
    name = "主程序自学习引擎"
    version = "1.0.0"
    
    def __init__(self, memory: MemoryService, model: ModelService,
                 logger: Optional[LoggerService] = None):
        self.memory = memory
        self.model = model
        self.logger = logger
    
    async def learn(self, context: Dict[str, Any]) -> Optional[str]:
        """预留接口"""
        return await self.daily_review()
    
    async def daily_review(self) -> Optional[str]:
        """
        夜间复盘
        
        第一期只需实现：
        1. 读取当天日志（logs/schedule/、logs/role/）
        2. 统计各角色任务数量、成功率
        3. 如 logger 存在，记录统计摘要
        4. 返回统计文本（暂不做 LLM 分析）
        
        第二期扩展：
        - 调用 LLM 分析调度失败根因
        - 生成 scheduling_patterns 更新建议
        - 更新 user_preference_profile
        """
        pass
    
    async def learn_scheduling(self) -> Optional[str]:
        """学习调度策略（预留）"""
        pass
    
    async def learn_user_preferences(self) -> Optional[str]:
        """学习用户偏好（预留）"""
        pass
```

### 3.7 `suri-agent/learning/learning.md`

```markdown
---
module_id: learning
name: 自学习模块
version: "1.0.0"
owner: suri-dev
---

# 自学习模块

## 定位

Suri 平台的经验积累与进化系统，包含角色自学习和主程序自学习两个子系统。

## 子模块

| 文件 | 职责 |
|------|------|
| `base.py` | 学习器抽象基类 |
| `feedback_collector.py` | 收集任务反馈数据 |
| `experience_extractor.py` | Prompt 工程 + LLM 调用 + 结果解析 |
| `role_learner.py` | 角色级经验提取、去重、存储 |
| `platform_learner.py` | 平台级统计与策略优化（框架预留） |

## 触发时机

1. 任务完成后（RoleLearner，异步）
2. 用户显式反馈后（FeedbackCollector，即时）
3. 夜间定时复盘（PlatformLearner，cron 触发）

## 存储位置

- 角色经验：`group/<role>/memories/insights/*.md`
- 角色模式：`group/<role>/memories/patterns/*.md`
- 平台经验：`suri-agent/memory/platform-learning/*.md`

## 接口

```python
# 主入口
from learning import RoleLearner

# 初始化
learner = RoleLearner(memory_service, model_service, logger_service)

# 触发学习（异步，不等待结果）
asyncio.create_task(learner.learn_from_task(role_id, task_id))
```

## 变更日志

| 日期 | 变更 |
|------|------|
| 2026-04-30 | 初始化模块 |
```

---

## 4. 现有文件改动清单（5 个）

### 4.1 `suri-agent/infrastructure/memory.py`

**新增导入**：
```python
import re
from typing import Set
```

**新增常量**：
```python
INSIGHT_CATEGORIES = {"success_pattern", "improvement", "pitfall", "preference"}
```

**新增方法**（在 `read_role_memory` 之后插入）：

```python
def _get_insights_dir(self, role_id: str) -> Path:
    """获取角色的经验目录"""
    insights_dir = self.project_root / 'group' / role_id / 'memories' / 'insights'
    insights_dir.mkdir(parents=True, exist_ok=True)
    return insights_dir

def save_role_insight(self, role_id: str, insight_data: dict) -> str:
    """
    保存角色学习经验
    
    Args:
        insight_data: {
            'title': str,
            'category': str,  # success_pattern / improvement / pitfall / preference
            'situation': str,
            'key_point': str,
            'avoid': str,
            'confidence': float,
        }
    
    Returns:
        保存的文件相对路径
    """
    insights_dir = self._get_insights_dir(role_id)
    
    # 文件名：YYYYMMDD_HHMMSS_{ sanitized_title }.md
    timestamp = datetime.now().strftime('%Y%m%d_%H%M%S')
    safe_title = re.sub(r'[^\w\u4e00-\u9fff]+', '_', insight_data['title'])[:30]
    filename = f"{timestamp}_{safe_title}.md"
    filepath = insights_dir / filename
    
    # 构建 Markdown 内容（含 YAML frontmatter）
    content = self._build_insight_markdown(insight_data, timestamp)
    filepath.write_text(content, encoding='utf-8')
    
    return str(filepath.relative_to(self.project_root))

def _build_insight_markdown(self, data: dict, timestamp: str) -> str:
    """构建经验文件的 Markdown 格式"""
    return f"""---
insight_id: ins_{timestamp}
category: {data['category']}
created_at: "{datetime.now().isoformat()}"
trigger_count: 1
confidence: {data.get('confidence', 0.5)}
ttl_days: 90
last_triggered: "{datetime.now().isoformat()}"
---

# {data['title']}

## 情境
{data['situation']}

## 要点
{data['key_point']}

## 避免
{data['avoid']}

## 验证记录
- [{datetime.now().strftime('%Y-%m-%d')}] 首次提取
"""

def list_role_insights(self, role_id: str, limit: int = 50) -> list:
    """
    按时间倒序列出角色的经验文件
    
    Returns:
        [{ 'path': str, 'meta': dict, 'content': str, 'created_at': str }]
    """
    insights_dir = self._get_insights_dir(role_id)
    if not insights_dir.exists():
        return []
    
    results = []
    for md_file in sorted(insights_dir.glob('*.md'), reverse=True)[:limit]:
        content = md_file.read_text(encoding='utf-8')
        meta, body = self._parse_insight_frontmatter(content)
        results.append({
            'path': str(md_file.relative_to(self.project_root)),
            'meta': meta,
            'content': body,
            'created_at': meta.get('created_at', '')
        })
    return results

def _parse_insight_frontmatter(self, content: str) -> tuple:
    """解析经验文件的 YAML frontmatter"""
    # 简单实现：按 "---" 分割，中间是 YAML，后面是 Markdown body
    # 可用正则或简单字符串处理，不引入 yaml 库依赖
    parts = content.split('---', 2)
    if len(parts) >= 3:
        meta_text = parts[1].strip()
        body = parts[2].strip()
        meta = self._simple_yaml_parse(meta_text)
        return meta, body
    return {}, content

def _simple_yaml_parse(self, text: str) -> dict:
    """极简 YAML 解析（只处理 key: value 单层）"""
    result = {}
    for line in text.strip().split('\n'):
        if ':' in line and not line.startswith('#'):
            key, val = line.split(':', 1)
            val = val.strip().strip('"').strip("'")
            # 尝试类型转换
            if val.lower() in ('true', 'false'):
                result[key.strip()] = val.lower() == 'true'
            else:
                try:
                    result[key.strip()] = float(val) if '.' in val else int(val)
                except ValueError:
                    result[key.strip()] = val
    return result

def get_recent_insights_for_context(self, role_id: str, task_hint: str = "", 
                                     limit: int = 5, max_chars: int = 2000) -> str:
    """
    获取用于注入上下文的经验文本
    
    筛选逻辑（方案 A 轻量版）：
    1. 按时间取最近 30 天内的经验
    2. 按 confidence 降序排列
    3. 如 task_hint 不为空，做简单关键词匹配粗排
    4. 截取前 limit 条，总字符不超过 max_chars
    
    Returns:
        格式化的经验文本块，可直接注入 System Prompt
    """
    insights = self.list_role_insights(role_id, limit=100)
    
    # 过滤 30 天内
    from datetime import datetime, timedelta
    cutoff = (datetime.now() - timedelta(days=30)).isoformat()
    recent = [i for i in insights if i.get('created_at', '') > cutoff]
    
    # 按 confidence 降序
    recent.sort(key=lambda x: x.get('meta', {}).get('confidence', 0), reverse=True)
    
    # 关键词粗排（如有 task_hint）
    if task_hint:
        recent = self._keyword_rank(recent, task_hint)
    
    # 组装并截断
    parts = []
    total = 0
    for ins in recent[:limit]:
        meta = ins.get('meta', {})
        title = meta.get('category', 'insight').replace('_', ' ').title()
        block = f"### {title}: {ins['meta'].get('title', '未命名')}\n"
        block += f"{ins['content'][:300]}\n\n"  # 单条截断 300 字
        
        if total + len(block) > max_chars:
            break
        parts.append(block)
        total += len(block)
    
    return "\n".join(parts) if parts else ""

def _keyword_rank(self, insights: list, hint: str) -> list:
    """极简关键词匹配排序"""
    hint_words = set(hint.lower().split())
    scored = []
    for ins in insights:
        text = (ins.get('meta', {}).get('title', '') + ' ' + ins.get('content', '')).lower()
        score = sum(1 for w in hint_words if w in text)
        scored.append((score, ins))
    scored.sort(key=lambda x: x[0], reverse=True)
    return [i[1] for i in scored]

def update_insight_trigger(self, role_id: str, insight_path: str) -> None:
    """
    更新经验的触发计数（经验被注入上下文时调用）
    
    操作：
    1. 读取文件
    2. trigger_count + 1
    3. last_triggered 更新为当前时间
    4. 写回文件
    """
    pass
```

### 4.2 `suri-agent/core/context.py`

**修改 `build_context()` 方法**：

在第 3 步（文件权限地图）和第 4 步（历史记忆）之间，插入新步骤：

```python
def build_context(self, role_id: str, current_task: Optional[Dict[str, Any]] = None,
                  include_memories: bool = True) -> str:
    parts = []
    
    # 1. 角色 Soul（已有）
    soul = self.config.get_role_soul(role_id)
    ...
    
    # 2. 关键规则摘要（已有）
    rule_summary = self._get_rule_summary()
    ...
    
    # 3. 个人文件权限地图（已有）
    files_map = self._get_files_map(role_id)
    ...
    
    # ===== 新增：第 3.5 步 =====
    # 4. 学习经验（新增）
    learning_insights = self._get_learning_insights(role_id, current_task)
    if learning_insights:
        parts.append(f"## 你的经验总结\n\n{learning_insights}")
    # ===========================
    
    # 5. 历史记忆（已有，序号顺延）
    if include_memories:
        memories = self._get_relevant_memories(role_id, current_task)
        ...
    
    # 6. 当前任务（已有，序号顺延）
    ...
    
    return "\n\n---\n\n".join(parts)
```

**新增方法**：

```python
def _get_learning_insights(self, role_id: str, 
                           task: Optional[Dict[str, Any]]) -> str:
    """
    获取角色的学习经验，注入上下文
    
    调用 MemoryService 的筛选逻辑
    """
    task_hint = task.get('requirement', '') if task else ''
    return self.memory.get_recent_insights_for_context(
        role_id, 
        task_hint=task_hint,
        limit=5,
        max_chars=2000
    )
```

### 4.3 `suri-agent/core/task_dispatcher.py`

**修改 `dispatch()` 方法**：

在方法末尾、return 之前，添加异步触发：

```python
async def dispatch(self, task_id: str) -> Dict[str, Any]:
    # ... 原有逻辑 ...
    
    # 发送给总监（原有）
    await self.comm.send_to_role(target_director, msg)
    
    # ===== 新增：异步触发学习 =====
    # 注意：需要在 __init__ 中注入 learner 实例
    if hasattr(self, '_learner') and self._learner:
        import asyncio
        asyncio.create_task(
            self._learner.learn_from_task(target_director, task_id)
        )
    # ==============================
    
    return {'success': True, 'task_id': task_id, ...}
```

**修改 `__init__`**：

```python
def __init__(self, config: ConfigService, memory: MemoryService,
             context: ContextService, model: ModelService, 
             comm: CommService, logger=None, learner=None):
    # ... 原有参数 ...
    self.logger = logger
    self._learner = learner  # 新增：学习引擎实例
```

**注意**：`main.py` 中初始化 `TaskService` 时需要传入 `learner` 参数。

### 4.4 `suri-agent/infrastructure/logger.py`

**新增方法**（在 `log_doc_sync` 之后）：

```python
def log_learning(self, role_id: str, event: str, detail: str = "") -> None:
    """记录自学习事件"""
    detail_str = f" | {detail}" if detail else ""
    self.runtime("信息", "自学习", f"[{role_id}] {event}{detail_str}")

def log_learning_error(self, role_id: str, error: str) -> None:
    """记录自学习错误"""
    self.error_log("错误", "自学习", f"[{role_id}] {error}")
    self.runtime("错误", "自学习", f"[{role_id}] {error}")
```

### 4.5 `suri-agent/main.py`

**修改 `initialize()` 方法**：

在步骤 11（TaskService 初始化）之前，插入学习引擎初始化：

```python
async def initialize(self) -> bool:
    # ... 步骤 1-10 保持不变 ...
    
    # ===== 新增：步骤 10.5 =====
    # 初始化自学习引擎
    from learning import RoleLearner
    self.learner = RoleLearner(self.memory, self.model, self.logger)
    print("[OK] 自学习引擎已启动")
    # ===========================
    
    # 11. 任务服务（修改：传入 learner）
    self.task = TaskService(
        self.config, self.memory, self.context, 
        self.model, self.comm, self.logger, 
        learner=self.learner  # 新增参数
    )
    print("[OK] 任务服务已启动")
    
    # ... 后续步骤 ...
```

---

## 5. 存储结构规范

### 5.1 角色经验文件格式

**路径**：`group/<department>/<role>/memories/insights/YYYYMMDD_HHMMSS_{title}.md`

**内容模板**：
```markdown
---
insight_id: ins_20260501_143022
category: success_pattern
created_at: "2026-05-01T14:30:22"
trigger_count: 1
confidence: 0.8
ttl_days: 90
last_triggered: "2026-05-01T14:30:22"
---

# 调度设计部任务时优先确认风格偏好

## 情境
当用户提出设计类需求（海报、Logo、配图）时适用。

## 要点
在生成任何视觉内容前，必须先向用户确认：1. 尺寸规格 2. 风格偏好（简约/复古/科技感）3. 用途场景（线上/印刷）。

## 避免
未确认风格直接生成，大概率导致用户反复修改，浪费双方时间。

## 验证记录
- [2026-05-01] 首次提取（任务 task_abc123）
```

**字段说明**：

| 字段 | 类型 | 说明 |
|------|------|------|
| insight_id | str | 唯一标识，格式 `ins_YYYYMMDD_HHMMSS` |
| category | str | `success_pattern` / `improvement` / `pitfall` / `preference` |
| created_at | ISO时间 | 创建时间 |
| trigger_count | int | 被注入上下文的次数 |
| confidence | float | 0-1，初始值：pitfall=0.8, success=0.7, improvement=0.6, preference=0.5 |
| ttl_days | int | 生存周期，默认 90 天 |
| last_triggered | ISO时间 | 上次被使用的时间 |

### 5.2 目录结构（运行时生成）

```
group/
└── <department>/<role>/
    ├── memories/
    │   ├── role.db              # SQLite（已有）
    │   ├── *.md                 # 私人记忆（已有）
    │   ├── insights/            # 🆕 经验洞察
    │   │   └── *.md
    │   ├── patterns/            # 🆕 压缩后的模式（夜间复盘生成）
    │   │   └── *.md
    │   ├── mistakes/            # 🆕 错误案例
    │   │   └── *.md
    │   └── _archive/            # 🆕 过期归档
    │       └── YYYY-MM.zip
    └── ...

suri-agent/memory/
├── ai-dev-memory/               # AI 开发记忆（已有）
└── platform-learning/           # 🆕 主程序学习记忆
    ├── scheduling_patterns.md
    ├── dept_matching_rules.md
    ├── user_preference_profile.md
    └── model_performance_log.md
```

---

## 6. 冗余控制机制（四层防御）

### 第一层：写入时去重（RoleLearner._is_duplicate）

**算法**：杰卡德系数（关键词集合交集/并集）

```python
def _jaccard_similarity(self, text1: str, text2: str) -> float:
    """计算两段文本的关键词相似度"""
    # 1. 分词：提取 2 字以上的中文词和英文单词
    words1 = self._extract_keywords(text1)
    words2 = self._extract_keywords(text2)
    
    # 2. 计算杰卡德系数
    if not words1 or not words2:
        return 0.0
    intersection = len(words1 & words2)
    union = len(words1 | words2)
    return intersection / union if union > 0 else 0.0

# 阈值：similarity > 0.6 视为重复
```

**合并规则**（`_merge_insight`）：
- `trigger_count` + 1
- `last_triggered` 更新为当前时间
- `confidence` = max(旧 confidence, 新 confidence) × 0.9 + 0.1（验证一次略微提升）
- 在"验证记录"段追加新行

### 第二层：夜间压缩（PlatformLearner.daily_review 扩展）

第二期实现，第一期预留接口。

### 第三层：动态遗忘（基于 meta 字段）

**遗忘规则**：
- `confidence < 0.2` 且 `trigger_count == 0` → 删除
- `created_at` 超过 `ttl_days` 且 `trigger_count < 2` → 移入 `_archive/`
- `trigger_count >= 5` 且 `confidence >= 0.8` → 升级至 `patterns/`

**实施时机**：`PlatformLearner.daily_review()` 中执行清理。

### 第四层：上下文注入截断（get_recent_insights_for_context）

已实现（见 4.1）：
- 30 天过滤
- confidence 排序
- 关键词粗排
- 硬截断 2000 字符

---

## 7. 错误处理规范

| 场景 | 处理方式 | 日志级别 |
|------|---------|---------|
| LLM 调用失败（网络/超时） | 放弃本次学习，不保存 | error |
| LLM 返回无法解析 | 记录原始响应，放弃保存 | error |
| 文件写入失败（权限/磁盘满） | 重试 1 次，仍失败则放弃 | error |
| 去重检测失败 | 当作新经验写入（允许少量重复） | warn |
| 读取经验文件失败（损坏） | 跳过该文件，继续处理其他 | warn |
| 学习过程整体崩溃 | 捕获异常，不影响主流程 | error |

**核心原则**：学习是"锦上添花"，绝不能影响主业务流程。

---

## 8. 测试验证清单

实施完成后，必须验证以下 10 项：

| # | 验证项 | 测试方法 | 通过标准 |
|---|--------|---------|---------|
| 1 | 模块可导入 | `python -c "from learning import RoleLearner"` | 无报错 |
| 2 | 经验文件生成 | 完成一个模拟任务，检查文件系统 | `group/<role>/memories/insights/` 下出现 `.md` 文件 |
| 3 | 文件格式正确 | 读取生成的 `.md` 文件 | 包含 YAML frontmatter + 情境/要点/避免三段 |
| 4 | 上下文注入 | 调用 `ContextService.build_context()` | 返回值包含 `"## 你的经验总结"` |
| 5 | 注入长度受控 | 生成 20 条经验后调用 build_context | 经验段字符数 ≤ 2000 |
| 6 | 异步非阻塞 | 测量 `TaskService.dispatch()` 返回时间 | < 500ms（学习在后台） |
| 7 | 日志记录 | 检查 `logs/runtime/` | 包含 `"[自学习]"` 开头的条目 |
| 8 | 去重生效 | 两次相同任务后检查文件数 | insights 目录下不重复生成相同标题文件 |
| 9 | 错误恢复 | 模拟 LLM 调用失败 | 主流程不受影响，日志记录错误 |
| 10 | 平台学习框架 | 调用 `PlatformLearner.daily_review()` | 不报错，返回统计文本（可为空） |

---

## 9. 与现有架构的兼容性说明

| 现有模块 | 影响 | 说明 |
|---------|------|------|
| `ConfigService` | 无修改 | 学习模块不依赖配置扫描 |
| `SecurityService` | 无修改 | 经验文件属 `role_self`，权限已有 |
| `FileService` | 无修改 | 经验写入走 MemoryService，不直接操作文件 |
| `DocWatcher` | 无冲突 | 学习写入 `memories/insights/` 不在监控范围内 |
| `ModelService` | 需确保 async | `call_model_async` 或兼容接口 |
| `rules/` | 无修改 | 学习是上层功能，不改动规则引擎 |
| `process/` | 无修改 | 学习不走流程引擎 |

---

## 10. 交付物检查表

交给另一个 AI 后，产出应包含：

- [ ] `suri-agent/learning/` 目录（7 个文件）
- [ ] `suri-agent/infrastructure/memory.py`（扩展方法）
- [ ] `suri-agent/core/context.py`（注入经验段）
- [ ] `suri-agent/core/task_dispatcher.py`（异步触发）
- [ ] `suri-agent/infrastructure/logger.py`（学习日志）
- [ ] `suri-agent/main.py`（初始化 learner）
- [ ] 至少 1 个角色目录下生成测试用的 `insights/*.md`
- [ ] 全部 10 项测试验证通过
- [ ] `suri-agent/learning/learning.md` 与代码一致

---

*本规格书基于 Suri Agent v1.0.0 架构编写，适用于方案 A（轻量版）实施。*

---

> **归档状态**: ✅ 已完成开发并归档
> **归档时间**: 2026-05-01
> **实现位置**: 见 development-plan/TASK_BOARD.md
