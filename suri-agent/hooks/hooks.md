# hooks/

事件钩子：拦截文件操作、触发回调、监控文件系统变更。

## 模块说明

| 文件 | 功能 |
|------|------|
| `doc_watcher.py` | 文档监控钩子：后台监控 suri-agent/、group/、wiki/ 文件变更，维护待同步队列，驱动 DocSyncRule 自动检测 |

## 设计原则

- 轻量级：只记录变更事件，不阻塞主程序
- 延迟检测：文件保存后延迟扫描，避免临时文件干扰
- 去重：同一目录的多次变更只记录一次

## 事件记录

- 新增 `doc_watcher.py` 文档监控钩子
