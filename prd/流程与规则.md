






流程规则

先定流程，再将规则。

所有接入又suri炸

流程

运行时全链路

用户发送给suri

关键词：

会话初始化（链接大模型调用回答，没有平台提示）
意外断开模型（模型使用规则）
消息会话终端（支持渠道终端、telegram多会话，独立上下文）发送消息有模型

- 会话中断（用户主动、服务升级）


开发全链路

ide开发

更新介绍文档（每次开发完成后阅读介绍文档是否跟更改）

介绍文档（介绍文件夹下功能）

运行时开发


角色学习模块：负责角色学习机制

插件升级插件；当被插件调用时候，读取所有用户与插件相关记忆，决定插件优化方向。

p