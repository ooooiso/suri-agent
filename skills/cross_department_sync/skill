---
skill_id: cross_department_sync
name: 跨部门协作同步
owner: suri
version: "0.1.0"
status: active
---

# 跨部门协作同步

## 功能概述

协调多部门参与的复杂任务，管理各方进度，汇总阶段性成果，确保协作链信息透明。

## 触发条件

- task_dispatch 识别到需求涉及多个部门
- 用户明确要求多部门协作

## 输入

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| task_id | string | 是 | 主任务 ID |
| involved_departments | array | 是 | 涉及部门 ID 列表 |
| requirement | string | 是 | 原始需求描述 |

## 执行步骤

1. **确定总监列表**：从 `function_index.md` 获取各部门 `lead_role`。
2. **发起协作**：向各方总监发送协作请求（私聊），抄送调度群。
3. **建立同步通道**：为本次协作创建临时进度跟踪（可存入 `state.db`）。
4. **收集进度**：定期向各方总监请求进度更新。
5. **汇总交付**：整合各方产出，生成统一交付物，向用户汇报。

## 输出

| 字段 | 类型 | 说明 |
|------|------|------|
| sync_id | string | 协作事件 ID |
| status | string | in_progress / completed / failed |
| deliverable | object | 汇总后的交付物 |

## 异常处理

- 某方总监未响应：触发 escalation，考虑临时指定代理或调整协作范围。
