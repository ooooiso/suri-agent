---
skill_id: task_dispatch
name: 任务分派
owner: suri
version: "0.1.0"
status: active
---

# 任务分派（suri 二次调度）

## 功能概述

suri 的核心调度技能。接收主程序转发的用户需求后，进行二次分析，通过读取 `function_index.md` 匹配责任部门与总监，生成结构化任务消息并下发。

## 触发条件

- 主程序（`suri-agent/main.py`）从接入层（终端/Telegram）接收到用户消息后，将消息转发给 suri。
- suri 激活此技能进行二次调度。

## 输入

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| raw_input | string | 是 | 用户原始自然语言需求 |
| user_id | string | 是 | 用户唯一标识 |
| source | string | 是 | 消息来源：terminal / telegram / tui |
| timestamp | string | 是 | 需求接收时间 |

## 执行步骤

1. **需求解析**：提取关键词、任务类型、紧急程度、隐含部门倾向。
2. **读取索引**：加载 `suri/function_index.md`，比对各部门 `function` 字段。
3. **匹配部门**：计算关键词与部门职能的相似度，确定责任部门。
4. **定位总监**：从匹配部门中获取 `lead_role`。
5. **生成任务消息**：构造标准消息格式（含 `message_id`、`task_ref`、`receiver_role`、`body`）。
6. **下发总监**：发送给目标总监，抄送中台调度群。

## 二次调度示例

| 用户输入 | suri 分析 | 调度目标 |
|---------|----------|---------|
| "开发一个新功能" | 关键词：开发、功能 → 匹配开发部 | `dev_lead`（达芬奇） |
| "画一张海报" | 关键词：画、海报 → 匹配设计部 | `art_director`（香奈儿） |
| "帮我查一下竞品数据" | 关键词：查、数据、竞品 → 匹配研究/运维 | `ops_admin`（居里） |

## 依赖工具

| 工具 ID | 来源 | 说明 |
|---------|------|------|
| text_classifier | `tools/data_converter/` | 需求文本分类 |

## 输出

| 字段 | 类型 | 说明 |
|------|------|------|
| task_id | string | 全局唯一任务 ID |
| target_department | string | 目标部门 ID |
| target_director | string | 目标总监 role_id |
| dispatch_message | object | 已发送的标准消息 |

## 异常处理

- **无法匹配部门**：向用户请求澄清，提供可选部门列表。
- **多部门需求**：触发 `cross_department_sync` 技能，进入跨部门协作流。
- **总监离线**：触发 `escalation` 技能，升级至部门群广播。

## 关联流程

- 开发人员遇到问题 → 触发 `escalation` → 回流用户 → 用户决策 → 继续实现 → 技能沉淀
