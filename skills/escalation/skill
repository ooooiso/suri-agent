---
skill_id: escalation
name: 任务升级
owner: suri
version: "0.1.0"
status: active
---

# 任务升级

## 功能概述

处理任务执行过程中的异常与延迟，按重试策略逐级升级，最终回流用户请求决策。

## 触发条件

- 任务执行失败
- 成员/总监超时未响应
- 重试次数耗尽

## 输入

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| task_id | string | 是 | 关联任务 ID |
| current_status | string | 是 | 当前状态（failed / timeout / rejected） |
| retry_count | int | 是 | 已重试次数 |
| last_error | string | 否 | 最后一次错误信息 |

## 执行步骤

1. **判断升级路径**：
   - 成员失败 → 重试（最多 3 次）
   - 重试耗尽 → 汇报总监
   - 总监无法解决 → 汇报 suri
   - suri 无法解决 → 回流用户
2. **执行重试**：按 `scheduling.md` 间隔重试（0s, 30s, 120s）。
3. **生成升级报告**：汇总失败原因、重试记录、当前处理人。
4. **回流用户**：向用户发送报告，请求重新规划或取消。

## 输出

| 字段 | 类型 | 说明 |
|------|------|------|
| escalation_id | string | 升级事件 ID |
| final_status | string | resolved / user_decision_required |
| report | object | 升级报告（含原因、重试记录、建议） |

## 异常处理

- 用户 24 小时未响应：标记任务为 `stalled`，归档至 `memories/`。
