---
skill_id: exception_handler
name: 通用异常处理
owner: suri
version: "0.1.0"
status: active
---

# 通用异常处理

## 功能概述

捕获并分类平台运行过程中的各类异常，根据异常类型路由到对应的处理流程（重试、升级、告警、忽略）。

## 触发条件

- 任何角色上报异常
- 系统级错误（模型超时、文件 IO 错误、网络断开等）

## 输入

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| error_type | string | 是 | 异常类型 |
| source_role | string | 是 | 异常来源角色 |
| task_id | string | 否 | 关联任务 ID |
| error_message | string | 是 | 错误详情 |
| stack_trace | string | 否 | 堆栈信息 |

## 执行步骤

1. **分类异常**：
   - `model_timeout` → 触发模型降级（model_routing）
   - `permission_denied` → 记录安全日志，通知 security_admin
   - `task_failed` → 触发 escalation
   - `system_error` → 通知 ops_admin
2. **路由处理**：调用对应技能或发送告警。
3. **记录日志**：写入 `logs/exception.log`。

## 输出

| 字段 | 类型 | 说明 |
|------|------|------|
| handler_action | string | 处理动作 |
| routed_to | string | 接收处理的角色/系统 |

## 异常处理

- 异常处理本身失败：紧急通知 ops_admin 和 config_admin；若 5 分钟内无响应，由 suri 直接向用户发送系统告警。
